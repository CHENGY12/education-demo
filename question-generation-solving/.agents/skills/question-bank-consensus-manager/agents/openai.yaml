interface:
  display_name: "题库共识审校 Manager"
  short_description: "用三个独立解题 Agent 与教师 Agent 批量审校题库"
  default_prompt: "Use $question-bank-consensus-manager to run the target directories end to end, preserve every unresolved seed in the Git-portable human-review queue, replace only rejected generated questions, validate provenance, and prepare a manifested clean delivery when requested."
